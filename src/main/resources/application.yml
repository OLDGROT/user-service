spring:
  application:
    name: "astonLearn"
  profiles:
    active: dev
  jmx:
    enabled: false
  hal:
    enabled: true
    jackson:
      serialization:
        fail-on-empty-beans: false

app:
  kafka:
    topic:
      user-events: user.events

